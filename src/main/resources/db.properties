# Local development only. Every value here is overridden by an environment
# variable of the same purpose in any deployed environment, which is why the
# defaults in Db.java are neutral rather than convenient.
#
# Credentials live here rather than in Java because a core that has to be
# edited when a database is renamed is not independent of it, and the seam
# test enforces exactly that by reading the source.
admin.url=jdbc:postgresql://localhost:5436/postgres
db.url=jdbc:postgresql://localhost:5436/agentic_visitors
db.user=minimart
db.password=minimart
